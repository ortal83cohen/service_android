<?xml version="1.0" encoding="utf-8"?>
<com.hpe.sb.mobile.app.common.utils.AppBar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/app_bar"
    windowActionBarOverlay="true"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/activeRequestCardSecondary"
    android:theme="@style/Base.ThemeOverlay.AppCompat.Dark.ActionBar"
    android:windowActionBarOverlay="true"
    app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
    app:subtitleTextAppearance="?attr/subtitleTextAppearance"
    android:paddingTop="@dimen/status_bar_height"
    app:titleMarginBottom="0dp"
    app:titleTextAppearance="?attr/titleTextAppearance" >
    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"

        android:id="@+id/search_pane_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:clickable="true"
        android:focusable="true"
        android:focusableInTouchMode="true">

    <com.hpe.sb.mobile.app.common.uiComponents.metricFont.MetricFontCustomTextView
        android:id="@+id/title"
        style="@style/TextStyle.Header"
        android:text="@string/log_in"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:textAllCaps="true" />
    </FrameLayout>
</com.hpe.sb.mobile.app.common.utils.AppBar>
